import { SearchestadoPipe } from './searchestado.pipe';

describe('SearchestadoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchestadoPipe();
    expect(pipe).toBeTruthy();
  });
});
