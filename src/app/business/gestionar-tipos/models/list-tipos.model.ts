export interface tipo {
    id_tipo?: number;
    nombre: string;
    estado: number;
    sw: number;
}

export interface tipoNotUndefined {
    id_tipo: number;
    nombre: string;
    estado: number;
    sw: number;
}